const BOOKING_CATEGORIES = Object.freeze({
  MASCOTS: 'mascots',
  DECORATIONS: 'decorations',
  BOUNCE_BUBBLE: 'bounce-bubble',
});

const BOOKING_CATEGORY_VALUES = Object.freeze(Object.values(BOOKING_CATEGORIES));

module.exports = {
  BOOKING_CATEGORIES,
  BOOKING_CATEGORY_VALUES,
};